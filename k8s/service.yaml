apiVersion: v1
kind: Service
metadata:
  name: k8s-guide-svc
  namespace: k8s-guide
spec:
  type: LoadBalancer
  selector:
    app: k8s-guide
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
